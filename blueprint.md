
FairyBiome
==========
## 概要
```
sequenceDiagram
participant Graphql@{ "type" : "database" }
participant Firestore@{ "type" : "database" }
participant Store@{ "type" : "database" }
participant PartWorkers@{ "type" : "collections" }
participant Main
actor User/Ecosys
    Graphql->>+Store:static
    Firestore->>+Store: learned
    Store->>+Store:preprocess
    User/Ecosys->>+Main: speech/message
    Main->>+PartWorkers: broadcast
    Store->>+PartWorkers: matrix
    PartWorkers->>+Main: inner speeches
    Main->>+User/Ecosys: outer speech
```
ユーザや人工環境からのメッセージをMainが受取り、それをPartにbroadcastし所定時間待機する。
Partはそのアルゴリズムに合わせたStoreからデータを受け取って返答を生成し、内言としてbroadcastする。
Mainは待機中に集まった内言を集約して一つを選び発言とする。
Partが使用する辞書のうち、staticな部分はgraphqlから提供、学習した内容はFirestoreから提供し
アルゴリズムに合わせたStore上で予備的な計算を行っておく。

## ページ構成
### /
* firestoreを利用したユーザ認証をする。
* アプリのランディングページでプライベートのチャットルームを開くボタンがある。
* ユーザのアバター、表示名、背景色の設定画面
* オプション(?debug)をつけるとチャットボット管理画面にアクセスできる

### /room
* firestoreを利用したユーザ認証をする。
* チャットボットが起動していなければ / にリダイレクトする
* 個人用のチャットルームで人工環境がレンダリングされた環境で、チャットボットとの会話ができる。

### /room?id=000
* firestoreを利用したユーザ認証をする。
* 共通のチャットルームで複数のユーザやチャットボットと会話できる

## 人工環境
機能
* 仮想の人工環境(天候、昼夜)を提供

開発方針
* ブラウザ上でインスタンスは一つ(worker化)
* チャットルーム環境、チャットボットとの通信にはchannelを利用する


## チャットルーム環境 /room
機能
* スマートホンでの表示を前提としブラウザ上で動作する。人工環境に連動した背景などをレンダリング
* チャットボットとの会話が可能(firestoreを会話ログのサーバとする)
* ノンバーバルコミュニケーションを重視するため、チャットボットとユーザのアバターを会話のフローと別に大きめにレンダリング
* チャットルーム上での複数メンバー間+チャットボットでの会話(firestoreを会話ログのサーバとする)

開発方針
* 人工環境がなくても動作する
* チャットボットがなくても動作する
* 個人ルームを開発・リリース後、共有のチャットルームを作る
* 人工環境、チャットボットとの通信はchannelで行う。

### チャットボット
機能
* ユーザがキャラクタの実在感を感じられることを目的として、下記のような動作をする。
  - アプリのスタートアップ画面がチャットボットを実行するタブとなり、キャラクタごとにWebLockで多重起動を禁止する。
  - 出現するかしないかはチャットボットが決める。(必ず出現しなくても良い)
  - 退室もチャットボットが決める。
  - チャットルームをユーザが閉じてもスタートアップ画面が残っていたら退室メッセージが残せる
* いたずら心、好奇心、競争心、自制心、射幸心、自尊心、遊び心、各話題など「心のパート」ごとに細分化された
  パートのチャットボットが競争的・共同的に動作して返答を生成する。
* 各パートのチャットボットはアルゴリズムが異なっても良い。
* 表情ごとにアバターを切り替えられる。
* 返答を計算するための特徴量としてアバター(表情)、時刻、天候、ユーザ発言、自分の発言などを利用可能

開発方針
* ブラウザ上で計算を行い、学習辞書はfirestoreを介して共有。
* 辞書のオリジナルはgraphqlで供給し、学習部分のみfirestoreに保存。 
* パートの数は可変。運用しながら新しいパートを追加する。
* 複数のパートが並列処理されることから専用workerを利用しbroadcastChannelで入力、内言、外言のレンダリングなどを行う。
  * アプリのスタートアップ画面はオプションをつけて起動した場合チャットボット管理画面となり、デバッグモードとして
  一つのパートを非workerとして手動で起動/終了できる。このモードでは生データを出力する簡易なUIを提供する。
* 人工環境、チャットルームとの通信は専用のchannelを利用する。


## firestore上のDBの設計方針
### チャットルームのログ


### 学習辞書
1. リッチログ

* キャラクタごと、かつ日付ごとにファイルを分割して管理。
* logドキュメントにはテキスト+特徴量(日時情報、天候、話し手ID、アバターの表情、聞き手IDなど)を保存。
  logドキュメントは追記専用
* 行ごとに内言で使われた回数、外言で使われた回数を計数しstatドキュメントに保存。
* アプリのタブが破棄された瞬間に送信できない想定なので、logやstatはindexedDBに蓄えておき、
  次回のアプリ起動時にfirestoreに一括送信します。
* ドキュメント ID（logId）はクライアント生成（ULID/UUID）

2. 統計情報
